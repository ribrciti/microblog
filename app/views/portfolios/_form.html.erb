<%= form_with(model: @portfolio_item, local: true) do |form| %>

  <% if @portfolio_item.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@portfolio_item.errors.count, "error") %> prohibited thisportfolio from being saved:</h2>

      <ul>
      <% @portfolio_item.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

   <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, size: "50" %>
  </div>

   <div class="form-group">
    <%= form.label :subtitle %>
    <%= form.text_field :subtitle, size: "47.5" %>
  </div>

  <div class="form-group">
    <%= form.label :body %>
    <%= form.text_area :body, size: "50 x 10" %>
  </div>

  <h2>Technologies Used:</h2>

  <div class="form-control">
    <ul>
      <%= form.fields_for :technologies do |technology_form| %>
        <li>
          <%= technology_form.label :name %>
          <%= technology_form.text_field :name, size: "40"  %>
        </li>
      <% end %>
    </ul>
  </div>
  <br>
  <br>
  <br>
  <br>
  <br>

   <div class="form-group">
    <%= form.label :thumb_image %>
    <%= form.text_field :thumb_image, size: "90"  %>
  </div>

  <div class="form-group">
    <%= form.label :main_image %>
    <%= form.text_field :main_image, size: "90"  %>
  </div>

  <div class="form-group">
    <%= form.submit %>
  </div>
<% end %>



